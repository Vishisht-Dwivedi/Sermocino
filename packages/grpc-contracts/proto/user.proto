syntax = "proto3";

package user;

service UserService {
  rpc CreateProfile (CreateProfileRequest) returns (CreateProfileResponse);
}

message CreateProfileRequest {
  string user_id = 1; // auth-service user id
  string email = 2;
}

message CreateProfileResponse {
  bool success = 1;
}