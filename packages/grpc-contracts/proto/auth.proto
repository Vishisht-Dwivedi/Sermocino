syntax = "proto3";

package auth;

service AuthService {
  rpc GenerateUser (GenerateRequest) returns (GeneratedResponse);
}

message ValidateRequest {
  string token = 1;
}

message ValidateResponse {
  bool valid = 1;
  string userId = 2;
}